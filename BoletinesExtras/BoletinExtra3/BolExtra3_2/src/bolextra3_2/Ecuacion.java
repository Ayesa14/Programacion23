package bolextra3_2;

import java.text.DecimalFormat;

/*public class Ecuacion {
    private double a, b, c;
    
    public void ecuacion(double a, double b, double c){
        double x = ((-b) + Math.sqrt(Math.pow(b, 2) - (4* a * c))) / (2 * a);
        double xm = ((-b) - Math.sqrt(Math.pow(b, 2) - (4* a * c))) / (2 * a);
        
        do 
        {
            double x = ((-b) + Math.sqrt(Math.pow(b, 2) - (4* a * c))) / (2 * a);
            double xm = ((-b) - Math.sqrt(Math.pow(b, 2) - (4* a * c))) / (2 * a);   
        }
        while 
                
        DecimalFormat df = new DecimalFormat();
        System.out.println("El primer valor de x es: "+df.format(x));
        System.out.println("El segundo valor de x es: "+df.format(xm));
    }
}*/
